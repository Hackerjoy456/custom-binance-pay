# ============================================
# Vercel Environment Variables
# Only needed for the Binance API proxy (api/index.js)
# Add these in Vercel Dashboard → Settings → Environment Variables
# ============================================

# ── Optional: Proxy Authentication Secret ────────────
# Set this to prevent unauthorized use of your proxy.
# Must match the PROXY_SECRET in Lovable Cloud secrets.
PROXY_SECRET=your_random_secret_here
